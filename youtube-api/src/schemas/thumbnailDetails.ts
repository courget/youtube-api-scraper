import thumbnail from "./thumbnail";

const thumbnailDetails = {
  default: thumbnail,
  high: thumbnail,
  maxres: thumbnail,
  medium: thumbnail,
  standard: thumbnail,
};

export default thumbnailDetails;
